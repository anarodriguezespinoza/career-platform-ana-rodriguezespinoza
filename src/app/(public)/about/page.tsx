import { SourceStatus } from "../../../components/public/source-status";
import { getPublicContent } from "../../../lib/public/content";
import { metadataFor } from "../../../lib/public/metadata";
export const metadata = metadataFor({ title: "About — Ana Rodriguez", description: "Professional summary and contact details for Ana Rodriguez.", path: "/about" });
export default async function AboutPage() { const { content, source } = await getPublicContent(); const profile = content.profile; return <div className="page-shell py-16 sm:py-24"><SourceStatus source={source} /><div className="max-w-3xl"><p className="eyebrow">About</p><h1 className="page-title">A practical idealist who likes making complex things clear.</h1><p className="mt-8 text-xl leading-9 text-[var(--muted)]">{profile?.summary ?? "Professional summary unavailable."}</p><dl className="mt-12 grid gap-8 border-t border-[var(--line)] pt-8 sm:grid-cols-2"><div><dt className="eyebrow">Location</dt><dd className="mt-2 text-lg">{profile?.location ?? "Available remotely"}</dd></div><div><dt className="eyebrow">Contact</dt><dd className="mt-2 text-lg"><a className="text-[var(--accent)] underline decoration-[var(--accent)]/30 underline-offset-4" href={profile?.email ? `mailto:${profile.email}` : "mailto:hello@example.com"}>{profile?.email ?? "hello@example.com"}</a></dd></div></dl></div></div>; }
