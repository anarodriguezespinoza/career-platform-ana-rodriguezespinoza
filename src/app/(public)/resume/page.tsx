import { SourceStatus } from "../../../components/public/source-status";
import { getPublicContent } from "../../../lib/public/content";
import { metadataFor } from "../../../lib/public/metadata";
export const metadata = metadataFor({ title: "Resume — Ana Rodriguez", description: "Resume and professional profile for Ana Rodriguez.", path: "/resume" });
export default async function ResumePage() { const { content, source } = await getPublicContent(); const resume = content.resumeSettings; const downloadUrl = source === "database" && content.profile && resume ? "/api/resume" : resume?.resumeUrl; return <div className="page-shell py-16 sm:py-24"><SourceStatus source={source} /><p className="eyebrow">Resume</p><h1 className="page-title">{resume?.title ?? "Resume"}</h1><p className="mt-8 max-w-2xl text-xl leading-9 text-[var(--muted)]">{resume?.intro ?? content.profile?.summary ?? "A concise overview of my experience and skills."}</p>{downloadUrl ? <a href={downloadUrl} className="button-primary mt-10 inline-flex">Download resume</a> : <p className="mt-10 text-sm text-[var(--muted)]">A downloadable resume is available on request.</p>}</div>; }
