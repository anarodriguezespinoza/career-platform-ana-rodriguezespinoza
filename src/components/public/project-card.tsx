import Link from "next/link";
import type { PublicProject } from "../../domain/content/types";
export function ProjectCard({ project }: { project: PublicProject }) { return <article className="flex h-full flex-col border border-[var(--line)] bg-white p-6 shadow-[0_10px_30px_rgba(31,41,51,0.04)] transition-transform hover:-translate-y-1"><div className="mb-6 flex items-start justify-between gap-4"><span className="eyebrow">Project</span><span className="font-mono text-xs text-[var(--muted)]">{String(project.displayOrder + 1).padStart(2, "0")}</span></div><h3 className="font-serif text-2xl font-semibold text-[var(--ink)]"><Link href={`/projects/${project.slug}`} className="hover:text-[var(--accent)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[var(--accent)]">{project.name}</Link></h3><p className="mt-3 flex-1 leading-7 text-[var(--muted)]">{project.description}</p><div className="mt-6 flex flex-wrap gap-2">{project.technologies.map((technology) => <span className="tag" key={technology.technology}>{technology.technology}</span>)}</div></article>; }
